# 02-AC1 smoke refuses a non-local database, commit f73f2b3, local

$ DATABASE_URL=postgres://u:p@example.com/db pnpm smoke
FAIL - DATABASE_URL must point at a local database (localhost, 127.0.0.1 or [::1]) with DATABASE_DRIVER not "neon"; smoke resets every seeded War Week and never runs against a hosted database
exit=1

$ DATABASE_URL='postgres://u:p@localhost/db?host=prod.example.com' pnpm smoke   # host override (review fix)
FAIL - DATABASE_URL must point at a local database (localhost, 127.0.0.1 or [::1]) with DATABASE_DRIVER not "neon"; smoke resets every seeded War Week and never runs against a hosted database
exit=1

# The guard is the first statement in main(), before the .next check and pnpm db:migrate, so nothing is migrated.
